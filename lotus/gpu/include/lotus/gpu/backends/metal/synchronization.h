#pragma once

/// \file
/// Metal synchronization primitives.

#include <cstddef>

namespace lotus::gpu::backends::metal {
	// TODO
	class fence {
	protected:
		fence(std::nullptr_t); // TODO
	};

	// TODO
	class timeline_semaphore {
	protected:
		timeline_semaphore(std::nullptr_t); // TODO
	};
}
